---
session: 49fcfbfd-a708-43d4-89ed-2068c2b33652
captured: 2026-06-01T16:55:37+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-nitra-ci-shared/49fcfbfd-a708-43d4-89ed-2068c2b33652.jsonl
---

[thinking]
The transcript shows a session where the user had an npm publish error due to the `bin` field in `package.json` having a `./`-prefixed path (`./src/cli.mjs`), which npm@11 considered invalid and auto-removed during publish. The fix was to remove the `./` prefix. Let me write the ADR.
</thinking>

## ADR bin-поле без `./`-префікса у `package.json`

## Context and Problem Statement
npm@11 (`npm warn publish "bin[ci-shared]" script name src/cli.mjs was invalid and removed`) автоматично видаляв запис `bin["ci-shared"]` з пакета під час `npm publish`, бо значення `./src/cli.mjs` починалось із `./`. CLI-запуск через `npx @nitra/ci-shared` ставав неможливим після публікації.

## Considered Options
* Зберігати шлях `bin` без `./`-префікса (`src/cli.mjs`)
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Зберігати шлях `bin` без `./`-префікса (`src/cli.mjs`)", because npm@11 відхиляє значення `./src/cli.mjs` як невалідне ім'я скрипта і видаляє весь запис із публікованого пакета; після виправлення `npm publish --dry-run` більше не виводив `warn publish` і `bin` зберігався.

### Consequences
* Good, because transcript фіксує очікувану користь: `npm publish --dry-run` після заміни не містить жодного `warn publish` про `bin`, пакет виходить з коректним `bin["ci-shared"]`.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- Файл: `npm/package.json`, поле `bin["ci-shared"]`: `"./src/cli.mjs"` → `"src/cli.mjs"`
- Версія npm, що відтворює проблему: `11.12.1` (Node `v26.0.0`)
- Перевірочна команда: `npm publish --dry-run` — після виправлення `warn`-рядки про `bin` зникли
- CI-workflow: `.github/workflows/npm-publish.yml`, використовує `JS-DevTools/npm-publish@v4.1.5` з OIDC (без npm-токена), проблема стосувалась лише поля `bin`, а не аутентифікації
