---
session: d378d31b-990b-4fb4-a544-ec1035382ddb
captured: 2026-05-12T12:33:13+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-ci-docs/d378d31b-990b-4fb4-a544-ec1035382ddb.jsonl
---

Транскрипт описує повну ініціалізацію npm-пакету `@nitra/ci-docs` та підгонку репозиторію під усі правила `@nitra/cursor`. Це кілька взаємопов'язаних дизайнових рішень.

## ADR Введення `bunfig.toml` з `linker = "hoisted"` для Bun monorepo

**Контекст:** Bun monorepo `ci-docs` не мав `bunfig.toml`, через що `@nitra/cursor check` повідомляв про відсутній hoisted-linker — без нього workspace-пакети можуть не знаходити спільні `node_modules`.

**Рішення/Процедура/Факт:** Створено `/Users/vitaliytv/www/nitra/ci-docs/bunfig.toml` з єдиним блоком `[install] linker = "hoisted"`.

**Обґрунтування:** Правило `n-bun.mdc` вимагає цього файлу явно; hoisted-режим гарантує, що всі workspace-пакети бачать одні `node_modules` у корені.

**Розглянуті альтернативи:** не обговорювалися.

**Зачіпає:** `bunfig.toml`, `bun.lock`, workspace `npm/`.

---

## ADR Канонічна структура `.github/workflows/` для npm-модуля

**Контекст:** Репозиторій мав лише `clean-merged-branch.yml` та `npm-publish.yml`; правило `n-ga.mdc` вимагає наявності `clean-ga-workflows.yml`, `clean-merged-branch.yml`, `lint-ga.yml`, `git-ai.yml`, а також окремих `lint-js.yml` та `lint-text.yml`.

**Рішення/Процедура/Факт:** Створено або переписано сім workflow-файлів. `clean-merged-branch.yml` — канонічна назва і `workflow_dispatch: {}`. `clean-ga-workflows.yml` — прибирає завершені runs. `lint-ga.yml` — actionlint + zizmor. `git-ai.yml` — автоматичний changelog при merge PR; виправлено shellcheck SC2086 (лапки навколо `$GITHUB_PATH`). `lint-js.yml` — oxlint + eslint + jscpd. `lint-text.yml` — cspell + shellcheck + markdownlint + v8r. Додано `.github/zizmor.yml` з політикою `ref-pin`. `npm-publish.yml` доповнено кроком `depcheck`.

**Обґрунтування:** Правила `n-ga.mdc`, `n-js-lint.mdc`, `n-text.mdc` та `n-npm-module.mdc` явно перераховують обов'язкові workflow; CI не може перевіряти код без цих файлів.

**Розглянуті альтернативи:** не обговорювалися.

**Зачіпає:** `.github/workflows/*.yml`, `.github/zizmor.yml`, `package.json` (скрипт `lint-ga`).

---

## ADR Канонічна конфігурація oxlint та ESLint для workspace

**Контекст:** `.oxlintrc.json` був застарілим кастомним файлом; `eslint.config.js` не мав ignores для `auto-imports.d.ts`; пакет `@nitra/eslint-config` не був встановлений.

**Рішення/Процедура/Факт:** `.oxlintrc.json` замінено копією `node_modules/@nitra/cursor/scripts/utils/oxlint-canonical.json` (включає плагін `vue` та `@e18e/eslint-plugin`). `eslint.config.js` доповнено `ignores: ['**/auto-imports.d.ts']`. Встановлено `@nitra/eslint-config` та `@nitra/cspell-dict` через `bun i`. Створено `.jscpd.json` з `exitCode: 1` та `minLines: 25`.

**Обґрунтування:** Правило `n-js-lint.mdc` вимагає canonical oxlint-конфіга від `@nitra/cursor`; `auto-imports.d.ts` — згенерований файл, що хибно спрацьовує на eslint-правилах.

**Розглянуті альтернативи:** не обговорювалися.

**Зачіпає:** `.oxlintrc.json`, `eslint.config.js`, `.jscpd.json`, `package.json`, `bun.lock`.

---

## ADR Структура npm-workspace: types, hk.pkl, jsconfig, publishConfig

**Контекст:** Workspace `npm/` мав лише порожній `src/schema.js`; правило `n-npm-module.mdc` вимагає `npm/types/index.d.ts`, `hk.pkl` з pre-commit tsc-кроком та `jsconfig.json`; пакет мав публікуватись як публічний.

**Рішення/Процедура/Факт:** Створено `npm/types/index.d.ts` (placeholder `export {}`), `npm/jsconfig.json` (NodeNext, esnext), `hk.pkl` з pre-commit кроком `tsc`. У `npm/package.json` додано `publishConfig.access: "public"`, поле `types: "./types/index.d.ts"`, `engines.bun >=1.3`. `npm/src/schema.js` наповнено `export {}` щоб уникнути помилки oxlint `unicorn/no-empty-file`.

**Обґрунтування:** Для скоупованого пакету `@nitra/*` npm за замовчуванням публікує як private; `publishConfig.access: "public"` — єдиний спосіб зробити його публічним без прапорця CLI. `hk.pkl` забезпечує перевірку типів перед кожним commit без CI-залежності.

**Розглянуті альтернативи:** Прапорець `--access public` в CLI команді або в step `JS-DevTools/npm-publish` — відхилено на користь декларативного підходу в `package.json`.

**Зачіпає:** `npm/package.json`, `npm/types/index.d.ts`, `npm/jsconfig.json`, `hk.pkl`, `npm/src/schema.js`.

---

## ADR Канонічні конфіги text-linting: cspell, markdownlint, v8r, oxfmt

**Контекст:** Проєкт не мав `.markdownlint-cli2.jsonc`, `.v8rignore`; `.oxfmtrc.json` не мав `ignorePatterns`; `.cspell.json` мав застарілий `import` без `bun.lock` в `ignorePaths`; `.vscode/settings.json` не мав `formatOnSave` та per-language formatter для `github-actions-workflow`.

**Рішення/Процедура/Факт:** Створено `.markdownlint-cli2.jsonc` з вимкненими MD013/MD029/MD040/MD041. Створено `.v8rignore` для виключення `.vscode/*.json`. Оновлено `.oxfmtrc.json`: додано `ignorePatterns` для `hasura/metadata`, `schema.graphql`, `auto-imports.d.ts`. Оновлено `.cspell.json`: видалено `yarn.lock` з ignorePaths, очищено `import`. Оновлено `.vscode/settings.json` з `editor.formatOnSave: true` та formatters для css, github-actions-workflow, json, markdown. Оновлено `.vscode/extensions.json`: додано markdownlint, shellcheck, redhat.vscode-yaml. Додано `.claude/hooks/capture-decisions.log` до `.gitignore`.

**Обґрунтування:** Правило `n-text.mdc` вимагає всі ці конфіги; v8r валідує JSON/YAML-файли за schema.org та не повинен чіпати vscode-специфічні файли без схем.

**Розглянуті альтернативи:** не обговорювалися.

**Зачіпає:** `.markdownlint-cli2.jsonc`, `.v8rignore`, `.oxfmtrc.json`, `.cspell.json`, `.vscode/settings.json`, `.vscode/extensions.json`, `.gitignore`.
