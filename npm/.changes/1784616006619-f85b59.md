---
bump: minor
section: Changed
---
Адаптовано sync-schema до `@graphql-inspector/core` v7: `diff` з `DiffRule.simplifyChanges` (без вкладених змін при додаванні вузла). `graphql` лишається на лінії 16.x (`^16.14.2`): peerDeps core не включають GraphQL 17, а спільне використання схем з двох копій graphql ламає `diff` («another module or realm»).
